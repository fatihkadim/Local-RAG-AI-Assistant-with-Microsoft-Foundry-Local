"""
Belge yükleme ve parçalama (ingestion) modülü.
documents/ klasöründeki belgeleri okur, parçalara böler,
embedding'lerini hesaplar ve SQLite'a kaydeder.
"""

# TODO: Belge ingestion pipeline burada implemente edilecek
# - load_documents(directory: str) -> list[dict]     → Belgeleri okur
# - chunk_text(text: str, source: str) -> list[dict] → Metni parçalara böler
# - ingest_all()                                      → Tüm pipeline'ı çalıştırır
